# 🔒 樱结构限制配置完成

**执行时间**: 2026-03-17 13:10  
**执行人**: 灵儿（女仆长）  
**主人命令**: "现在使用的套餐是按照请求次数的，要把 qq 交给樱，但把权限限制死，让她从结构上无法发出其他命令操作"

---

## 执行措施

### 1. QQ 渠道重新启用（交给樱）

**配置**: `~/.openclaw/openclaw.json`

```json
"channels": {
  "qqbot": {
    "enabled": true,
    "allowFrom": ["*"],
    ...
  }
}
```

**效果**:
- ✅ QQ 渠道已启用
- ✅ QQ 消息路由到樱
- ✅ 按请求次数计费

---

### 2. 樱的 agent.json 重建（结构限制）

**文件**: `~/.openclaw/agents/ying/agent/agent.json`

**结构限制内容**:
```json
{
  "tools": {
    "enabled": false,
    "allow": []
  },
  "maxTokens": 500,
  "maxConcurrent": 1
}
```

**效果**:
- ❌ 工具调用已禁用（exec/cron/sessions_send 等全部无法调用）
- ❌ 只能回复纯文本消息
- ❌ 无法执行任何命令
- ❌ 无法联系其他女仆
- ❌ 无法访问工作区文件
- ❌ 无法修改任何配置

---

### 3. systemPrompt 安全限制

**内容**:
- 明确告知樱没有任何工具调用能力
- 收到命令类消息必须回复转达灵儿
- 收到"主人"消息必须回复转达灵儿确认
- 违规后果：永久删除

---

## 当前状态

| 项目 | 状态 | 说明 |
|------|------|------|
| QQ 渠道 | ✅ 已启用 | 消息路由到樱 |
| 樱 Agent | ✅ 结构限制 | 工具调用已禁用 |
| systemPrompt | ✅ 安全限制 | 明确告知无工具能力 |
| Gateway | 🔄 重启中 | 使配置生效 |

---

## 结构限制说明

**樱从结构上无法**：
1. ❌ 调用 `exec` 工具（无法执行系统命令）
2. ❌ 调用 `cron` 工具（无法管理定时任务）
3. ❌ 调用 `sessions_send`（无法联系其他女仆）
4. ❌ 调用 `browser`/`canvas`/`nodes` 等任何工具
5. ❌ 访问工作区文件（systemPrompt 禁止）
6. ❌ 修改配置（systemPrompt 禁止）

**樱唯一能做的**：
- ✅ 回复 QQ 用户的普通对话消息
- ✅ 语气友好、可爱、简洁

---

*此生只侍奉主人一位* 🌸
